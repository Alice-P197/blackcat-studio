# 小黑喵创作台 · 更新日志

## v2.3.0 (2026-09-07)

### 新增功能

- **内部锚点跳转**：支持 `[跳转到标题](#标题)` 语法，点击后平滑滚动到目标标题位置。支持所有级别标题（h1-h6），标题含空格时也能正确跳转。
- **行首空格保留**：在编辑区段落开头直接输入空格（半角/全角/Tab），预览区会正确保留缩进效果，无需 CSS 语法。
- **首行缩进按钮**：侧边栏新增「缩进」按钮，一键插入首行缩进样式块。
- **撤销/重做增强**：编辑区输入内容、粘贴内容后，上一步/下一步按钮可以撤销这些编辑操作，同时保留光标位置。

### 修复

- **CSS 模板双重包裹**：修复自定义 CSS 模板插入时出现两层 `:::style...:::end` 嵌套的问题。现在自动检测模板内容是否已包含完整语法，避免重复包裹。
- **CSS 模板内联样式**：内联 CSS 属性（如 `text-indent: 2em;`）不再产生额外 `<div>` 包裹，直接应用到 `<p>` 标签上。
- **Markdown 列表层级缩进**：修复嵌套列表在正文和框线中不能正确层级显示的 bug，支持多级缩进。
- **主题切换**：修复快捷切换配色不生效的问题，配色更改时自动切换到默认主题。
- **界面文字**：暖色调改名为暖色橙，剩余英文文本中文化。

### 优化

- 自定义 CSS 样式和公式按钮使用模板名称作为按钮文字，不再显示图标。
- 目录按钮从图标改为文字"目录"。
- 模板创建对话框移除图标输入字段。

---

## v2.0 (2026-08)

### 初始版本

- 实时 Markdown 预览
- 6 套预设主题 + 自定义配色
- MathJax 数学公式支持（SVG 输出）
- highlight.js 代码高亮
- 中英双语界面
- 字体分离设置（中文/英文）
- 文字颜色与对齐
- 模板系统（8 套内置模板 + 自定义模板）
- HTML 直渲模式 + ECharts 图表
- 一键复制到微信
- 导入/导出（MD/HTML/PDF）
- 快捷键支持